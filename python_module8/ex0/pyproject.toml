[project]
name = "ex0"
version = "1.1.1"
description = ""
authors = [
    {name = "maderrab"}
]
requires-python = ">=3.13"

dependencies = [
    "numpy (>=2.4.4,<3.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
